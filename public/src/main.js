// main.js is intentionally minimal. Index HTML now contains the markup
// and /portfolio.css contains the styling. Use this file for small client-side
// interactivity or to import additional JS modules.

if (import.meta.hot) {
  import.meta.hot.accept()
}
